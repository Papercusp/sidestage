{
  "name": "@papercusp/sidestage-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "start:dev": "npm run build --workspace @papercusp/system-test-contract && npm run build --workspace @papercusp/sse && npm run build --workspace @papercusp/typesense && tsx watch --exclude '../../node_modules/**' src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build --workspace @papercusp/system-test-contract && npm run build --workspace @papercusp/sse && npm run build --workspace @papercusp/typesense",
    "test": "vitest run --project sidestage-node",
    "test:file": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.29",
    "@papercusp/sse": "*",
    "@papercusp/sync": "*",
    "@papercusp/system-test-contract": "*",
    "@papercusp/typesense": "*",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6",
    "tsx": "^4.23.12"
  }
}
