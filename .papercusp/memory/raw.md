[2026-08-14T05:17:26Z] content-fixer -: ts-parse content-guard detector false-positives on standard NestJS @Inject(...) constructor-param decorators (TS1206) — it's not respecting the owning package's experimentalDecorators:true tsconfig; verified via build_typecheck (0 errors) and prevalence of the same pattern across apps/api/src. Escalated rather than 'fixed' by rewriting DI style.
[2026-08-14T05:34:00Z] content-fixer -: re-dispatched for the same sync.controller.ts ts-parse quarantine (TS1206 decorator false-positive, see prior note) but by the time I ran, git status was clean and apps/api tsc -p tsconfig.json --noEmit passed with 0 errors — the files were already committed (7f72816). No edit made; confirms the false-positive is transient/detector-config-dependent rather than a real syntax bug. If this recurs, check git status first before re-fixing.
[2026-08-14T05:49:04Z] content-fixer -: 3rd recurrence of the ts-parse @Inject(...) decorator false-positive (stats.module.ts, build-history.module.ts) — this time filed a proper bug (EI-20406141251368744, dup of EI-20402162039851642) + coord:escalate (conditionKey sidestage-git-sync-ts-parse-decorator-false-positive) instead of just a memory note, since it keeps recurring and the detector fix (add experimentalDecorators:true to ts-parse.ts phase-2 CompilerOptions) is still outstanding. No file edits made.
[2026-08-25T09:01:30Z] judge -: judge principal has NO ledger reads (testing:runs → operator:read denied; code:structural → intel:read denied; work_items:get/plans:list → role_not_allowed) and Bash is policy-forbidden — verify via direct file Reads only: count the tests in cited files and cross-check against the frozen audit's claimed pass counts (an inventory mismatch would expose a fabricated claim). Filed EI-21414925819818618.
[2026-08-27T23:00:40Z] release-fixer -: Shared test-config submodule bumps can red-gate managed hives when a shared unit test reads parent-project-only paths; pin the prior published submodule until a topology-aware upstream fix exists.
[2026-08-28T01:16:45Z] release-fixer WI-307959: A submodule inside a linked superproject worktree has a gitdir shaped .git/worktrees/<worktree>/modules/<path>; root classifiers must treat the modules suffix as submodule evidence before accepting the worktrees marker as the repo root.
[2026-09-01T03:07:00Z] content-fixer -: For generated build-history snapshots, scripts/lib/identity-leak-patterns.mjs redactIdentityLeaks(text, []) applies the exact owner-tag/home-path detector fixes; after redaction the shared absolute-file probe verifies all registered detectors without hand-editing serialized payloads.
